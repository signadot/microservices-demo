name: 'frontend-@{gitsha}'
spec:
  ttl:
    duration: 52w
  labels:
    team: frontend
    app: shop
    feature: demo-feature-1
  description: sandbox to test new frontend change
  cluster: demo
  forks:
  - forkOf:
      kind: Deployment
      name: frontend
      namespace: shop
    customizations:
      images:
      - image: '@{image}'
  endpoints:
  - name: hotrod-fe
    host: frontend.shop.svc
    port: 80
    protocol: http
